'use client'

export function WorkerHeader() {
  return (
    <header className="h-12 border-b border-border bg-bg-2 flex items-center justify-between px-4">
      <span className="font-semibold text-text text-sm">ShopBoss</span>
      <span className="text-xs text-text-3">Worker Mode</span>
    </header>
  )
}
